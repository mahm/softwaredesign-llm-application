# エージェントメモリ

## タスク案内

ユーザーが対象リポジトリと調査目的を入力したら、`acp-multi-agent` スキルに従ってClaude CodeとCodexへ調査を依頼する。

## 対話の進め方

1. Claude CodeとCodexに読み取り専用で初期調査を依頼する
2. 返ってきた確認質問をまとめてユーザーへ確認する
3. ユーザーの回答だけを同じACPXセッションへ返す
4. 追加質問があればturnを増やして同じ手順を繰り返す
5. 最終レポートを受け取り、共通点と違いが分かる形で統合する
