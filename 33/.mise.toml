[env]
_.file = ".env"

[tools]
bun = "1.3.5"

[tasks.install]
description = "Install dependencies"
run = "bun install"

[tasks.start]
description = "Start the ACP server (for testing; normally launched by Zed)"
run = "bun run server.ts"
